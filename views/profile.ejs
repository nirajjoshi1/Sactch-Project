    <%- include('./partials/header') %>


<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile-@<%=user.fullname%></title>
    <style>
      * {
        font-family: sans-serif;
      }

      body {
        background: url(https://images.pexels.com/photos/1103970/pexels-photo-1103970.jpeg?)
          center bottom;
        background-attachment: fixed;
        object-fit: cover;
        background-size: cover;
        background-attachment: fixed;

      }

      .background {
        margin: 0%;
        padding: 0%;
        position: fixed;
        margin-top: 2%;
        margin-left: 4%;
        width: 100vw;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .transparent {
        height: 400px;
        width: 350px;
        margin: auto;
        justify-content: center;
        text-align: left;
        background-attachment: fixed;
        object-fit: cover;
        background-size: cover;
        background-attachment: fixed;
        border-radius: 10px;
        padding: 10px;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
      }

      p {
        color: #000;
        text-align: center;
        font-size: 20px;
        line-height: 25px;
        text-align: start;
      }
      img {
        height: 150px;
        width: 200px;
        border-radius: 50%;
        object-fit: cover;
        object-position: center;
        margin-bottom: 5px;
      }

      
    </style>
  </head>
  <% if(update.length>0){ %>
        <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-blue-500">
            <span class="inline-block mt-1 mb-1 text-white">
                <%= update %>
            </span>
        </div>
        <% } %>
      <div class=" px-10 py-10 flex flex-grow">
        <div class="background">
          <div class="transparent">
            <h1 class="text-2xl text-blue-600 font-bold mt-6">Your Profile</h1>
        <img class="h-[20rem]" src="data:image/jpeg;base64, <%=user.picture.toString('base64')%>" alt="Profile Image">
            <p><%=user.fullname%></p>
            <p><%=user.email%></p>
            <p><%=user.contact%></p>
          </div>
        </div>
      </div>
    </div>
     <main class="w-3/4 bg-white p-4 shadow ml-4 h-[70vh] ml-50">
                        <h2 class="text-2xl font-bold mb-2 text-blue-600">Update profile information</h2>
                        <form autocomplete="off" action="/update" method="post" enctype="multipart/form-data">
                            <div class="mb-">
                                    <label class="block mb-[1px] font-medium">Product Image</label>
                                    <input name="image" type="file" class="py-2 px-4 rounded" value="Select File">
                                </div>
                                <div class="info">
                                    <label class="block mb-[1px] font-medium">Name</label>
                                    <input class="px-5 py-2 rounded mt-3 bg-blue-500 text-white" type="text" name="fullname">
                                    <label class="block mb-[1px] font-medium">Contact</label>
                                     <input class="px-5 py-2 rounded mt-3 bg-blue-500 text-white" type="number" name="contact">
                                </div>
                            <input class="px-5 py-2 rounded mt-3 bg-blue-500 text-white" type="submit"
                                value="Update Profile">
                        </form>
                    </main>
  </body>
</html>

<%- include('./partials/footer') %>
